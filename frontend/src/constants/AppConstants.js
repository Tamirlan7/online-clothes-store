export const HOST_URL = process.env.NODE_ENV === 'production' ? `http://uncwear.store:8080` : `http://localhost:8080`
export const API_URL = `${HOST_URL}/api/v1`
export const TOKENS = 'tokens'
export const UNAUTHENTICATED_ENTRY = '/'
export const ADMIN_PRODUCTS_PAGE = 'admin_products_page'
